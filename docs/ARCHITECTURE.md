# Web3 EIP Playground - 整体架构文档

## 一、项目定位

**Web3 EIP Playground** 是一个通过游戏学习 ERC/EIP 标准的 Web3 教育平台。

核心理念：**Learn by Doing, Not Reading**

用户价值：

- 玩互动小游戏理解 EIP-1559、EIP-7702 等标准
- 阅读像素风漫画，降低学习门槛
- 向 AI 导师提问，获得通俗解答
- 通关后获得 NFT 成就徽章

---

## 二、项目结构

```
web3-eip-playground/
│
├── apps/
│   └── eip-playground/              # 前端 (Vite + React)
│       ├── client/                  # 前端源码
│       │   ├── src/
│       │   │   ├── pages/           # 页面 (Home, EIPDetail)
│       │   │   ├── components/      # 组件 (游戏、漫画、UI)
│       │   │   ├── lib/wagmi.ts     # 钱包配置
│       │   │   └── ...
│       │   └── public/              # 静态资源 (图片、漫画)
│       └── server/                  # Express 静态服务 (生产部署用)
│
├── spoon-core/                      # 后端 (Python FastAPI)
│   ├── spoon_ai/                    # AI Agent 模块 (已有)
│   │   ├── agents/
│   │   ├── tutor/
│   │   ├── llm/
│   │   └── ...
│   │
│   └── nft_service/                 # NFT 铸造模块 (新建)
│       ├── __init__.py
│       ├── router.py                # API 路由
│       ├── mint_service.py          # 铸造逻辑
│       ├── wallet.py                # Minter 钱包管理
│       └── models.py                # 数据模型
│
├── contract/                        # 智能合约 (Foundry)
│   ├── src/GameBadgeNFT.sol         # ERC-721 合约
│   ├── test/GameBadgeNFT.t.sol      # 测试
│   ├── script/Deploy.s.sol          # 部署脚本
│   ├── metadata/                    # NFT 元数据 (1.json ~ 9.json)
│   └── .env                         # 部署配置
│
└── docs/                            # 文档
    └── ARCHITECTURE.md
```

---

## 三、技术栈总览

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端** | Vite 7 + React 19 | 页面渲染 |
| | wagmi + RainbowKit | 钱包连接 |
| | viem | 链上读取 |
| | wouter | 路由 |
| | Tailwind CSS 4 | 样式 |
| | Framer Motion | 动画 |
| **后端** | FastAPI | API 框架 |
| | spoon-core (spoon_ai) | AI 导师 |
| | web3.py | 链上交互 (铸造) |
| **合约** | Solidity 0.8.24 | 智能合约 |
| | Foundry | 编译/测试/部署 |
| | OpenZeppelin | ERC-721 |
| **存储** | IPFS (Pinata) | NFT 元数据 |

---

## 四、整体架构图

```
                                 ┌──────────┐
                                 │   用户   │
                                 └────┬─────┘
                                      │
          ┌───────────────────────────┼───────────────────────────┐
          │                           │                           │
          ▼                           ▼                           ▼
   ┌─────────────┐            ┌─────────────┐            ┌─────────────┐
   │  连接钱包   │            │  学习游戏   │            │   AI 问答   │
   │ RainbowKit  │            │  闯关交互   │            │   导师解答  │
   └──────┬──────┘            └──────┬──────┘            └──────┬──────┘
          │                          │                          │
          └──────────────────────────┼──────────────────────────┘
                                     │
                                     ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                                                                            │
│                        前端 (apps/eip-playground)                          │
│                        Vite + React + wagmi                                │
│                                                                            │
│   ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐          │
│   │    首页    │  │  EIP 详情  │  │  互动游戏  │  │  我的 NFT  │          │
│   │   Hero     │  │ 漫画+游戏  │  │ GasWar 等  │  │  成就展示  │          │
│   └────────────┘  └────────────┘  └────────────┘  └────────────┘          │
│                                                                            │
│   wagmi/viem: 连接钱包、读取 tokenURI                                      │
│                                                                            │
└──────────────────────────────┬─────────────────────────────────────────────┘
                               │
                               │ HTTP API
                               ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                                                                            │
│                        后端 (spoon-core)                                   │
│                        Python FastAPI                                      │
│                                                                            │
│   ┌─────────────────────────────┐  ┌─────────────────────────────┐        │
│   │      nft_service (新建)     │  │      spoon_ai (已有)        │        │
│   │                             │  │                             │        │
│   │  POST /api/game/clear       │  │  POST /api/chat             │        │
│   │  • 验证通关                 │  │  • AI 导师问答              │        │
│   │  • 生成 claimId             │  │  • 解释 EIP 概念            │        │
│   │  • 调用合约 mintTo          │  │                             │        │
│   │                             │  │  GET /api/chat/history      │        │
│   │  GET /api/nft/list          │  │  • 对话历史                 │        │
│   │  • 查询用户 NFT             │  │                             │        │
│   │                             │  └─────────────────────────────┘        │
│   │  Minter 钱包 (web3.py)      │                                         │
│   │  • 私钥从环境变量读取       │                                         │
│   │  • 签名并发送交易           │                                         │
│   └─────────────────────────────┘                                         │
│                                                                            │
└──────────────────────────────┬─────────────────────────────────────────────┘
                               │
                               │ RPC (JSON-RPC)
                               ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                                                                            │
│                        区块链 (contract)                                   │
│                                                                            │
│   ┌──────────────────────────────────────────────────────────────────┐    │
│   │                      GameBadgeNFT.sol                            │    │
│   │                      ERC-721 可转移 NFT                          │    │
│   │                                                                  │    │
│   │  POOL_SIZE = 9          随机 index 范围 1-9                      │    │
│   │  mintTo(to, gameId, claimId)    铸造 NFT                         │    │
│   │  tokenURI(tokenId)              返回 ipfs://xxx/{index}.json     │    │
│   │  claimUsed[claimId]             防重放                           │    │
│   └──────────────────────────────────────────────────────────────────┘    │
│                                                                            │
└──────────────────────────────┬─────────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                        IPFS (Pinata)                                       │
│                                                                            │
│   ipfs://bafybeiadgwost5sefktvwsohhjtybut4n7ni3xeta2bwvifuxuumqtx3t4/     │
│   ├── 1.json  →  { name, image: "ipfs://xxx/1.png", ... }                 │
│   ├── 2.json                                                               │
│   ├── ...                                                                  │
│   └── 9.json                                                               │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 五、前端架构

### 设计风格：Retro-Futuristic Arcade

- **视觉**：8-bit/16-bit 像素风 + 霓虹灯效果
- **配色**：深空蓝紫背景 + 霓虹黄/青/品红高亮
- **交互**：游戏化体验，按钮像街机按键
- **字体**：Press Start 2P (标题) + 等宽字体 (正文)

### 页面结构

```
┌─────────────────────────────────────────────────────────────────────┐
│                              路由                                   │
├─────────────────────────────────────────────────────────────────────┤
│  /              →  Home.tsx        首页 (Hero + 功能网格)           │
│  /eip/:id       →  EIPDetail.tsx   EIP 详情页 (漫画 + 游戏 + 内容)   │
│  /nft           →  MyNFT.tsx       我的 NFT (待建)                  │
│  /404           →  NotFound.tsx    404 页面                         │
└─────────────────────────────────────────────────────────────────────┘
```

### 已实现的 EIP 内容

| EIP | 漫画 | 互动游戏 | 状态 |
|-----|------|----------|------|
| **EIP-1559** | ✅ 3页 | GasWarGame + BurnerGame | ✅ 完成 |
| **EIP-7702** | ✅ 3页 | BatchingGame + SponsorshipGame + DelegationGame | ✅ 完成 |
| **ERC-8004** | ❌ | ❌ | 📝 仅内容 |

### 前端组件结构

```
client/src/
├── App.tsx                    # 根组件，配置 Provider
├── main.tsx                   # 入口
├── index.css                  # 全局样式
│
├── pages/
│   ├── Home.tsx               # 首页
│   ├── EIPDetail.tsx          # EIP 详情页
│   └── NotFound.tsx           # 404
│
├── components/
│   ├── Header.tsx             # 顶部导航 (含钱包连接)
│   ├── Footer.tsx             # 底部
│   ├── Hero.tsx               # 首页 Hero
│   ├── FeatureGrid.tsx        # EIP 卡片网格
│   ├── ComicReader.tsx        # 漫画阅读器
│   │
│   ├── games/
│   │   ├── GasWarGame.tsx     # EIP-1559 旧方式演示
│   │   ├── BurnerGame.tsx     # EIP-1559 新方式演示
│   │   └── eip7702/
│   │       ├── BatchingGame.tsx
│   │       ├── SponsorshipGame.tsx
│   │       └── DelegationGame.tsx
│   │
│   └── ui/                    # Radix UI 组件库
│
├── data/
│   └── eips.ts                # EIP 数据定义
│
├── lib/
│   └── wagmi.ts               # wagmi 配置
│
└── contexts/
    ├── ThemeContext.tsx
    └── ScrollContext.tsx
```

### 钱包连接配置

```typescript
// lib/wagmi.ts
export const config = getDefaultConfig({
  appName: 'EIP Playground',
  projectId: 'xxx',
  chains: [mainnet, optimism, arbitrum, base, sepolia],
  ssr: false,
});
```

支持的链：Mainnet, Optimism, Arbitrum, Base, Sepolia

---

## 六、后端架构 (spoon-core)

### 模块划分

```
spoon-core/
├── spoon_ai/                # AI Agent 模块 (已有)
│   ├── agents/              # AI 代理
│   ├── tutor/               # AI 导师
│   ├── llm/                 # 多模型支持
│   ├── tools/               # 工具集
│   └── ...
│
└── nft_service/             # NFT 铸造模块 (新建)
    ├── __init__.py
    ├── router.py            # FastAPI 路由
    ├── mint_service.py      # 铸造逻辑
    ├── wallet.py            # Minter 钱包管理
    └── models.py            # 数据模型
```

### nft_service 模块设计

```python
# router.py
@router.post("/api/game/clear")
async def game_clear(request: GameClearRequest):
    """用户通关后请求铸造 NFT"""
    # 1. 验证请求
    # 2. 检查幂等
    # 3. 生成 claimId
    # 4. 调用合约 mintTo
    # 5. 返回结果

@router.get("/api/nft/list")
async def nft_list(user: str):
    """查询用户的 NFT 列表"""
    pass
```

```python
# mint_service.py
class MintService:
    def __init__(self, web3, contract, minter_private_key):
        self.web3 = web3
        self.contract = contract
        self.minter = Account.from_key(minter_private_key)

    def mint_to(self, to: str, game_id: int, claim_id: bytes32) -> TxReceipt:
        """调用合约铸造 NFT"""
        pass

    def generate_claim_id(self, user, game_id, run_id) -> bytes32:
        """生成唯一 claimId"""
        pass
```

---

## 七、智能合约

### GameBadgeNFT.sol

```solidity
contract GameBadgeNFT is ERC721, Ownable {
    uint8 public constant POOL_SIZE = 9;  // 9 种图案

    // 状态
    uint256 public nextTokenId;
    string public baseURI;
    address public minter;
    bool public paused;
    mapping(uint256 => uint8) public tokenToIndex;   // tokenId => index (1-9)
    mapping(bytes32 => bool) public claimUsed;       // 防重放

    // 核心函数
    function mintTo(address to, uint256 gameId, bytes32 claimId) external onlyMinter;
    function tokenURI(uint256 tokenId) public view returns (string memory);

    // 管理函数
    function setBaseURI(string calldata newBaseURI) external onlyOwner;
    function setMinter(address newMinter) external onlyOwner;
    function setPaused(bool paused_) external onlyOwner;

    // 内部函数
    function _randomIndex(...) internal view returns (uint8);  // 返回 1-9
}
```

### 部署配置

```bash
# contract/.env
BASE_URI="ipfs://bafybeiadgwost5sefktvwsohhjtybut4n7ni3xeta2bwvifuxuumqtx3t4/"
OWNER="0xYourOwnerAddress"
MINTER="0xYourMinterAddress"
START_TOKEN_ID=1
```

### NFT 元数据

```
IPFS CID: bafybeiadgwost5sefktvwsohhjtybut4n7ni3xeta2bwvifuxuumqtx3t4

文件列表:
├── 1.json (Common)
├── 2.json (Common)
├── 3.json (Common)
├── 4.json (Common)
├── 5.json (Uncommon)
├── 6.json (Uncommon)
├── 7.json (Uncommon)
├── 8.json (Uncommon)
└── 9.json (Legendary)

图片 CID: bafybeibiebovnb57w6jh6aqshp6ir6sofvm3c7gbd7asv2kdp6xdfqmniy
```

---

## 八、核心业务流程

### 用户学习 → 闯关 → 获得 NFT

```
  用户          前端            后端 (spoon-core)         合约           IPFS
   │             │                    │                    │              │
   │  打开应用   │                    │                    │              │
   │────────────►│                    │                    │              │
   │             │                    │                    │              │
   │  连接钱包   │                    │                    │              │
   │◄───────────►│ RainbowKit        │                    │              │
   │             │                    │                    │              │
   │  选择 EIP   │                    │                    │              │
   │────────────►│                    │                    │              │
   │             │                    │                    │              │
   │  看漫画     │                    │                    │              │
   │◄───────────►│                    │                    │              │
   │             │                    │                    │              │
   │  玩游戏     │                    │                    │              │
   │◄───────────►│                    │                    │              │
   │             │                    │                    │              │
   │  通关成功!  │                    │                    │              │
   │────────────►│                    │                    │              │
   │             │                    │                    │              │
   │             │  POST /api/game/clear                   │              │
   │             │  { userAddress, gameId, runId }         │              │
   │             │───────────────────►│                    │              │
   │             │                    │                    │              │
   │             │                    │ 1. 验证请求        │              │
   │             │                    │ 2. 检查幂等        │              │
   │             │                    │ 3. 生成 claimId    │              │
   │             │                    │                    │              │
   │             │                    │ 4. Minter 签名     │              │
   │             │                    │    mintTo(...)     │              │
   │             │                    │───────────────────►│              │
   │             │                    │                    │              │
   │             │                    │                    │ 5. 校验      │
   │             │                    │                    │ 6. 生成 index│
   │             │                    │                    │ 7. 铸造 NFT  │
   │             │                    │                    │ 8. 发事件    │
   │             │                    │                    │              │
   │             │                    │◄───────────────────│              │
   │             │                    │  Minted 事件       │              │
   │             │                    │                    │              │
   │             │                    │ 9. 解析事件        │              │
   │             │                    │ 10. 存入数据库     │              │
   │             │                    │                    │              │
   │             │◄───────────────────│                    │              │
   │             │  { tokenId, index, txHash }             │              │
   │             │                    │                    │              │
   │             │  tokenURI(tokenId) │                    │              │
   │             │─────────────────────────────────────────►              │
   │             │◄─────────────────────────────────────────              │
   │             │  ipfs://xxx/3.json │                    │              │
   │             │                    │                    │              │
   │             │  获取元数据        │                    │              │
   │             │─────────────────────────────────────────────────────────►
   │             │◄────────────────────────────────────────────────────────│
   │             │  { name, image }   │                    │              │
   │             │                    │                    │              │
   │◄────────────│                    │                    │              │
   │  展示 NFT   │                    │                    │              │
   │             │                    │                    │              │
```

### AI 导师问答

```
  用户                    前端                      后端                 spoon_ai
   │                       │                        │                        │
   │  "EIP-1559 是什么？"   │                        │                        │
   │ ─────────────────────►│                        │                        │
   │                       │  POST /api/chat        │                        │
   │                       │  { message, userId }   │                        │
   │                       │ ──────────────────────►│                        │
   │                       │                        │  调用 AI Agent         │
   │                       │                        │ ──────────────────────►│
   │                       │                        │                        │ LLM 处理
   │                       │                        │  ◄─── AI 回答 ─────────│
   │                       │  ◄─── 返回回答 ────────│                        │
   │  ◄─── 展示回答 ───────│                        │                        │
   │                       │                        │                        │
```

---

## 九、API 设计

### nft_service 模块

#### POST /api/game/clear

用户通关后请求铸造 NFT。

请求体：

```json
{
  "userAddress": "0x...",
  "gameId": "eip-1559",
  "runId": "uuid-xxx"
}
```

成功响应：

```json
{
  "success": true,
  "tokenId": 1,
  "index": 3,
  "txHash": "0x..."
}
```

已领取响应：

```json
{
  "success": true,
  "tokenId": 1,
  "index": 3,
  "message": "already_claimed"
}
```

#### GET /api/nft/list?user=0x...

查询用户拥有的 NFT 列表。

响应：

```json
{
  "nfts": [
    { "tokenId": 1, "index": 3, "gameId": "eip-1559", "mintedAt": "2025-01-29" },
    { "tokenId": 5, "index": 7, "gameId": "eip-7702", "mintedAt": "2025-01-30" }
  ]
}
```

### spoon_ai 模块

#### POST /api/chat

向 AI 导师提问。

请求体：

```json
{
  "message": "EIP-1559 的 Base Fee 是怎么计算的？",
  "userId": "xxx",
  "context": "eip-1559"
}
```

响应：

```json
{
  "reply": "Base Fee 根据上一个区块的 Gas 使用率动态调整...",
  "conversationId": "xxx"
}
```

---

## 十、各端职责划分

| 前端 (apps/eip-playground) | 后端 (spoon-core) | 合约 (contract) |
|---------------------------|-------------------|-----------------|
| **用户交互** | **nft_service** | **链上逻辑** |
| • 连接钱包 (RainbowKit) | • 验证通关请求 | • ERC-721 标准 |
| • 展示漫画 | • 幂等检查 | • onlyMinter 权限 |
| • 游戏闯关 | • 生成 claimId | • claimUsed 防重放 |
| • 通关调 API | • Minter 钱包管理 | • 随机 index (1-9) |
| | • 调用合约 mintTo | • tokenURI 返回 |
| **链上读取** | • 监听事件存库 | |
| • tokenURI | | **管理功能** |
| • 用户 NFT 列表 | **spoon_ai** | • setBaseURI |
| | • AI 导师问答 | • setMinter |
| **展示** | • 概念解释 | • setPaused |
| • NFT 图片 | | |
| • 对话界面 | **承担** | |
| | • Gas 费用 | |
| **不做** | • Minter 私钥安全 | |
| • 不发交易 | | |
| • 不管私钥 | | |

---

## 十一、安全要点

| 要点 | 说明 |
|------|------|
| **Minter 私钥** | 仅存储在后端环境变量中，不暴露给前端 |
| **Gas 费用** | 由项目方 Minter 钱包承担，用户无需付费 |
| **防重放** | 双重保障：后端幂等检查 + 合约 `claimUsed[claimId]` |
| **随机性** | 弱随机（block.prevrandao），适合展示类场景 |
| **通关验证** | 后端应验证用户确实完成了游戏，防止伪造请求 |
| **用户体验** | 用户全程无需签名交易，体验流畅 |
| **角色分离** | OWNER (管理) 和 MINTER (铸造) 分开，降低风险 |

---

## 十二、开发现状

| 模块 | 状态 | 说明 |
|------|------|------|
| **前端框架** | ✅ 完成 | Vite + React + wagmi + RainbowKit |
| **首页** | ✅ 完成 | Hero + FeatureGrid + Footer |
| **EIP 详情页** | ✅ 完成 | 漫画 + 游戏 + 内容展示 |
| **EIP-1559 游戏** | ✅ 完成 | GasWarGame + BurnerGame |
| **EIP-7702 游戏** | ✅ 完成 | BatchingGame + SponsorshipGame + DelegationGame |
| **漫画阅读器** | ✅ 完成 | ComicReader 组件 |
| **智能合约** | ✅ 完成 | GameBadgeNFT (POOL_SIZE=9, index 1-9) |
| **合约测试** | ✅ 完成 | 7 tests passed |
| **NFT 元数据** | ✅ 完成 | 1.json ~ 9.json 已上传 Pinata |
| **spoon_ai** | ✅ 已有 | AI Agent SDK |
| **nft_service** | ❌ 待建 | NFT 铸造模块 |
| **前端调 API** | ❌ 待建 | 通关后请求铸造 |
| **我的 NFT 页** | ❌ 待建 | 展示用户 NFT |
| **合约部署** | ❌ 待部署 | 需要 OWNER + MINTER 地址 |

---

## 十三、团队分工

| 成员 | 职责 |
|------|------|
| **Swen** | AI 导师 & 后端 (spoon-core + nft_service) |
| **David** | 前端 & 教育体验 (apps/eip-playground) |
| **Dudu** | 智能合约 & Web3 (contract) |

---

## 十四、下一步

1. **在 spoon-core 里新建 `nft_service/` 模块**
2. **实现 `/api/game/clear` 和 `/api/nft/list` 接口**
3. **前端对接 API（通关后请求铸造）**
4. **新建"我的 NFT"页面**
5. **部署合约**（需要 OWNER 和 MINTER 地址）
6. **联调测试**
